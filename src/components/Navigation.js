import React from "react";
import Navlinks from "./Navbar/Navlinks";

const Navigation = () => {
    return (
        <React.Fragment>
            <Navlinks />
        </React.Fragment>
    );
};

export default Navigation;
