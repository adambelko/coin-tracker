import React from "react";
import Navlinks from "./Navbar/Navbar";

const Navigation = () => {
    return (
        <React.Fragment>
            <Navlinks />
        </React.Fragment>
    );
};

export default Navigation;
